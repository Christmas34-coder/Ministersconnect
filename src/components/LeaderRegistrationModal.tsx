import React, { useState } from 'react';
import { X, Camera, Upload, Church, User, Mail, Phone, MapPin, FileText, Award, CheckCircle, AlertCircle, Sparkles, Globe, Briefcase
} from 'lucide-react';
import confetti from 'canvas-confetti';
import { MinisterialTitle, MinisterialPosition, ChurchLeader, MemberUser } from '../types';
import { addChurchLeader } from '../utils/storage';
import { PassportPhotoSelector } from './PassportPhotoSelector'; interface LeaderRegistrationModalProps { isOpen: boolean; onClose: () => void; onSuccess: (newLeader: ChurchLeader) => void; currentMember?: MemberUser | null;
} const MINISTERIAL_TITLES: MinisterialTitle[] = [ 'Pastor', 'Reverend', 'Bishop', 'Apostle', 'Evangelist', 'Prophet', 'Prophetess', 'Teacher', 'Deacon', 'Deaconess', 'Elder', 'Minister', 'Brother', 'Sister', 'Dr.', 'Other',
]; const PRESET_MINISTRY_FOCUS = [ 'Apostolic Preaching', 'Church Planting', 'Leadership Mentorship', 'Expository Teaching', 'Evangelistic Crusades', 'Youth & Campus Ministry', 'Praise & Worship Leadership', 'Intercession & Spiritual Warfare', 'Ministerial Family Health', 'Church Governance & Administration', 'Theological Education', 'Missions & Cross-Border Outreaches',
]; export const LeaderRegistrationModal: React.FC<LeaderRegistrationModalProps> = ({ isOpen, onClose, onSuccess, currentMember,
}) => { const [title, setTitle] = useState<MinisterialTitle>(currentMember?.title || 'Pastor'); const [fullName, setFullName] = useState(currentMember?.fullName || ''); const [email, setEmail] = useState(currentMember?.email || ''); const [phone, setPhone] = useState(currentMember?.phone || ''); const [whatsapp, setWhatsapp] = useState(currentMember?.whatsapp || currentMember?.phone || ''); const [churchName, setChurchName] = useState(currentMember?.churchName || ''); const [denomination, setDenomination] = useState(''); const [keyPosition, setKeyPosition] = useState('Senior Pastor & General Overseer'); const [positionDescription, setPositionDescription] = useState(''); const [selectedFocus, setSelectedFocus] = useState<string[]>([ 'Apostolic Preaching', 'Church Governance', ]); const [customFocus, setCustomFocus] = useState(''); const [yearsInMinistry, setYearsInMinistry] = useState<number>(10); const [city, setCity] = useState(currentMember?.city || ''); const [state, setState] = useState(currentMember?.state || ''); const [country, setCountry] = useState(currentMember?.country || 'Nigeria'); const [photoUrl, setPhotoUrl] = useState(currentMember?.avatarUrl || ''); const [websiteOrSocial, setWebsiteOrSocial] = useState(''); const [submitting, setSubmitting] = useState(false); const [errors, setErrors] = useState<Record<string, string>>({}); if (!isOpen) return null; const toggleFocus = (tag: string) => { if (selectedFocus.includes(tag)) { setSelectedFocus(selectedFocus.filter((t) => t !== tag)); } else { setSelectedFocus([...selectedFocus, tag]); } }; const handleAddCustomFocus = (e: React.KeyboardEvent) => { if (e.key === 'Enter' && customFocus.trim()) { e.preventDefault(); if (!selectedFocus.includes(customFocus.trim())) { setSelectedFocus([...selectedFocus, customFocus.trim()]); } setCustomFocus(''); } }; const validate = (): boolean => { const newErrors: Record<string, string> = {}; if (!fullName.trim()) newErrors.fullName = 'Leader Full Name is required'; if (!email.trim() || !/\S+@\S+\.\S+/.test(email)) newErrors.email = 'Valid Email is required'; if (!churchName.trim()) newErrors.churchName = 'Church or Ministry name is required'; if (!keyPosition.trim()) newErrors.keyPosition = 'Key Position / Ministerial Title is required'; if (!positionDescription.trim() || positionDescription.trim().length < 25) { newErrors.positionDescription = 'Please provide a brief description of your key position (minimum 25 characters)'; } if (!city.trim()) newErrors.city = 'City is required'; if (!photoUrl) newErrors.photoUrl = 'Please upload or capture your official ministerial portrait'; setErrors(newErrors); return Object.keys(newErrors).length === 0; }; const handleSubmit = (e: React.FormEvent) => { e.preventDefault(); if (!validate()) { return; } setSubmitting(true); setTimeout(() => { const newLeader = addChurchLeader({ title, fullName: fullName.trim(), email: email.trim().toLowerCase(), phone: phone.trim() || undefined, whatsapp: whatsapp.trim() || undefined, churchName: churchName.trim(), denomination: denomination.trim() || undefined, keyPosition: keyPosition.trim(), positionDescription: positionDescription.trim(), ministryFocus: selectedFocus.length > 0 ? selectedFocus : undefined, yearsInMinistry: Number(yearsInMinistry) || 1, city: city.trim(), state: state.trim() || undefined, country: country.trim(), photoUrl, websiteOrSocial: websiteOrSocial.trim() || undefined, memberId: currentMember?.id, }); confetti({ particleCount: 70, spread: 60, origin: { y: 0.6 }, colors: ['#f59e0b', '#d97706', '#10b981', '#6366f1'], }); setSubmitting(false); onSuccess(newLeader); onClose(); }, 450); }; return ( <div className="fixed inset-0 z-50 overflow-y-auto bg-slate-950/75 flex items-center justify-center p-3 sm:p-5"> <div className="bg-white border border-slate-200 rounded-2xl w-full max-w-3xl shadow-2xl overflow-hidden my-4 "> {/* Modal Header */} <div className="bg-gradient-to-r from-slate-900 via-slate-800 to-slate-900 text-white p-5 sm:p-7 relative border-b border-slate-700"> <button type="button" onClick={onClose} className="absolute top-4 right-4 w-8 h-8 rounded-full bg-white/10 hover:bg-white/20 text-slate-300 hover:text-white flex items-center justify-center transition cursor-pointer" > <X className="w-4 h-4" /> </button> <div className="inline-flex items-center gap-2 px-3 py-1 rounded-full bg-amber-500/20 text-amber-300 border border-amber-500/30 text-xs font-semibold uppercase tracking-wider mb-2"> <Award className="w-3.5 h-3.5" /> <span>Official Church Leadership Directory</span> </div> <h2 className="text-xl sm:text-2xl font-bold font-serif text-white"> Register as a Church Leader </h2> <p className="text-xs sm:text-sm text-slate-300 mt-1 max-w-2xl leading-relaxed"> Upload your ministerial portrait and provide a brief description of your key position and pastoral oversight in your church. </p> </div> {/* Modal Body Form */} <form onSubmit={handleSubmit} className="p-5 sm:p-7 max-h-[75vh] overflow-y-auto space-y-6"> {/* SECTION 1: Leader Photo & Identity */} <div className="space-y-4"> <h3 className="text-sm font-bold text-slate-900 uppercase tracking-wider flex items-center gap-2 border-b border-slate-100 pb-2"> <Camera className="w-4 h-4 text-amber-600" /> <span>1. Official Portrait & Salutation</span> </h3> {/* Passport Photo Selector */} <div> <PassportPhotoSelector value={photoUrl} onChange={(photo) => setPhotoUrl(photo)} label="Official Leader Portrait / High-Resolution Headshot" /> {errors.photoUrl && ( <p className="text-xs text-red-600 mt-1 flex items-center gap-1"> <AlertCircle className="w-3.5 h-3.5" /> {errors.photoUrl} </p> )} </div> <div className="grid grid-cols-1 sm:grid-cols-12 gap-3 sm:gap-4"> <div className="sm:col-span-4"> <label className="block text-xs font-semibold text-slate-700 mb-1"> Title / Salutation <span className="text-red-500">*</span> </label> <select value={title} onChange={(e) => setTitle(e.target.value as MinisterialTitle)} className="w-full px-3 py-2 rounded-xl border border-slate-300 text-sm font-medium bg-white focus:ring-2 focus:ring-amber-500" > {MINISTERIAL_TITLES.map((t) => ( <option key={t} value={t}>{t}</option> ))} </select> </div> <div className="sm:col-span-8"> <label className="block text-xs font-semibold text-slate-700 mb-1"> Full Name (Surname First or Preferred) <span className="text-red-500">*</span> </label> <input type="text" value={fullName} onChange={(e) => setFullName(e.target.value)} placeholder="e.g. Samuel Bukunmi Adeleke" className={`w-full px-3 py-2 rounded-xl border text-sm focus:ring-2 focus:ring-amber-500 ${ errors.fullName ? 'border-red-400 bg-red-50/20' : 'border-slate-300' }`} /> {errors.fullName && ( <p className="text-xs text-red-600 mt-1 flex items-center gap-1"> <AlertCircle className="w-3.5 h-3.5" /> {errors.fullName} </p> )} </div> <div className="sm:col-span-6"> <label className="block text-xs font-semibold text-slate-700 mb-1"> Email Address <span className="text-red-500">*</span> </label> <div className="relative"> <input type="email" value={email} onChange={(e) => setEmail(e.target.value)} placeholder="pastor@churchname.org" className="w-full pl-9 pr-3 py-2 rounded-xl border border-slate-300 text-sm focus:ring-2 focus:ring-amber-500" /> <Mail className="w-4 h-4 text-slate-400 absolute left-3 top-2.5" /> </div> {errors.email && ( <p className="text-xs text-red-600 mt-1 flex items-center gap-1"> <AlertCircle className="w-3.5 h-3.5" /> {errors.email} </p> )} </div> <div className="sm:col-span-6"> <label className="block text-xs font-semibold text-slate-700 mb-1"> Phone / WhatsApp Number </label> <div className="relative"> <input type="tel" value={phone} onChange={(e) => { setPhone(e.target.value); if (!whatsapp) setWhatsapp(e.target.value); }} placeholder="+234 803 123 4567" className="w-full pl-9 pr-3 py-2 rounded-xl border border-slate-300 text-sm focus:ring-2 focus:ring-amber-500" /> <Phone className="w-4 h-4 text-slate-400 absolute left-3 top-2.5" /> </div> </div> </div> </div> {/* SECTION 2: Church & Key Position Description */} <div className="space-y-4 pt-2 border-t border-slate-100"> <h3 className="text-sm font-bold text-slate-900 uppercase tracking-wider flex items-center gap-2 border-b border-slate-100 pb-2"> <Briefcase className="w-4 h-4 text-amber-600" /> <span>2. Church & Key Position Oversight</span> </h3> <div className="grid grid-cols-1 sm:grid-cols-2 gap-3 sm:gap-4"> <div> <label className="block text-xs font-semibold text-slate-700 mb-1"> Church / Ministry Name <span className="text-red-500">*</span> </label> <div className="relative"> <input type="text" value={churchName} onChange={(e) => setChurchName(e.target.value)} placeholder="e.g. Dominion Faith Bible Church" className={`w-full pl-9 pr-3 py-2 rounded-xl border text-sm focus:ring-2 focus:ring-amber-500 ${ errors.churchName ? 'border-red-400 bg-red-50/20' : 'border-slate-300' }`} /> <Church className="w-4 h-4 text-slate-400 absolute left-3 top-2.5" /> </div> {errors.churchName && ( <p className="text-xs text-red-600 mt-1 flex items-center gap-1"> <AlertCircle className="w-3.5 h-3.5" /> {errors.churchName} </p> )} </div> <div> <label className="block text-xs font-semibold text-slate-700 mb-1"> Denomination / Network Affiliation </label> <input type="text" value={denomination} onChange={(e) => setDenomination(e.target.value)} placeholder="e.g. Pentecostal / Apostolic Network" className="w-full px-3 py-2 rounded-xl border border-slate-300 text-sm focus:ring-2 focus:ring-amber-500" /> </div> <div className="sm:col-span-2"> <label className="block text-xs font-semibold text-slate-700 mb-1"> Key Position in Church <span className="text-red-500">*</span> </label> <input type="text" value={keyPosition} onChange={(e) => setKeyPosition(e.target.value)} placeholder="e.g. Senior Pastor & General Overseer, Presiding Bishop, National Youth Director" className={`w-full px-3 py-2 rounded-xl border text-sm font-semibold focus:ring-2 focus:ring-amber-500 ${ errors.keyPosition ? 'border-red-400 bg-red-50/20' : 'border-slate-300' }`} /> {errors.keyPosition && ( <p className="text-xs text-red-600 mt-1 flex items-center gap-1"> <AlertCircle className="w-3.5 h-3.5" /> {errors.keyPosition} </p> )} </div> {/* Description of Key Position */} <div className="sm:col-span-2"> <div className="flex items-center justify-between mb-1"> <label className="text-xs font-semibold text-slate-700"> Brief Description of Key Position & Pastoral Calling <span className="text-red-500">*</span> </label> <span className="text-[11px] text-slate-500"> {positionDescription.length} characters </span> </div> <textarea rows={4} value={positionDescription} onChange={(e) => setPositionDescription(e.target.value)} placeholder="Describe your ministerial responsibilities, spiritual calling, pastoral vision, oversight duties, and key contributions to your church and community (e.g. Provides chief apostolic direction, pulpit ministry, oversees 15 associate ministers, directs global missions and discipleship institute...)" className={`w-full p-3 rounded-xl border text-sm leading-relaxed focus:ring-2 focus:ring-amber-500 ${ errors.positionDescription ? 'border-red-400 bg-red-50/20' : 'border-slate-300' }`} /> {errors.positionDescription && ( <p className="text-xs text-red-600 mt-1 flex items-center gap-1"> <AlertCircle className="w-3.5 h-3.5" /> {errors.positionDescription} </p> )} </div> </div> </div> {/* SECTION 3: Ministry Focus Tags & Experience */} <div className="space-y-4 pt-2 border-t border-slate-100"> <h3 className="text-sm font-bold text-slate-900 uppercase tracking-wider flex items-center gap-2 border-b border-slate-100 pb-2"> <Sparkles className="w-4 h-4 text-amber-600" /> <span>3. Ministry Focus & Experience</span> </h3> <div> <label className="block text-xs font-semibold text-slate-700 mb-2"> Select Core Ministry Focus Areas (Click to toggle): </label> <div className="flex flex-wrap gap-1.5 mb-3"> {PRESET_MINISTRY_FOCUS.map((tag) => { const active = selectedFocus.includes(tag); return ( <button key={tag} type="button" onClick={() => toggleFocus(tag)} className={`px-3 py-1.5 rounded-lg text-xs font-medium transition cursor-pointer ${ active ? 'bg-amber-600 text-white shadow-xs' : 'bg-slate-100 text-slate-700 hover:bg-slate-200' }`} > {active ? '✓ ' : '+ '}{tag} </button> ); })} </div> <div className="flex items-center gap-2"> <input type="text" value={customFocus} onChange={(e) => setCustomFocus(e.target.value)} onKeyDown={handleAddCustomFocus} placeholder="Type custom focus tag & press Enter..." className="px-3 py-1.5 rounded-lg border border-slate-300 text-xs w-full sm:w-64 focus:ring-2 focus:ring-amber-500" /> <button type="button" onClick={() => { if (customFocus.trim() && !selectedFocus.includes(customFocus.trim())) { setSelectedFocus([...selectedFocus, customFocus.trim()]); setCustomFocus(''); } }} className="px-3 py-1.5 bg-slate-800 text-white rounded-lg text-xs font-semibold hover:bg-slate-700 cursor-pointer" > Add </button> </div> </div> <div className="grid grid-cols-1 sm:grid-cols-4 gap-3 sm:gap-4"> <div> <label className="block text-xs font-semibold text-slate-700 mb-1"> Years in Ministry </label> <input type="number" min="1" max="70" value={yearsInMinistry} onChange={(e) => setYearsInMinistry(parseInt(e.target.value) || 1)} className="w-full px-3 py-2 rounded-xl border border-slate-300 text-sm font-bold text-center focus:ring-2 focus:ring-amber-500" /> </div> <div> <label className="block text-xs font-semibold text-slate-700 mb-1"> City / Town <span className="text-red-500">*</span> </label> <input type="text" value={city} onChange={(e) => setCity(e.target.value)} placeholder="e.g. Ibadan" className="w-full px-3 py-2 rounded-xl border border-slate-300 text-sm focus:ring-2 focus:ring-amber-500" /> </div> <div> <label className="block text-xs font-semibold text-slate-700 mb-1"> State / Province </label> <input type="text" value={state} onChange={(e) => setState(e.target.value)} placeholder="e.g. Oyo State" className="w-full px-3 py-2 rounded-xl border border-slate-300 text-sm focus:ring-2 focus:ring-amber-500" /> </div> <div> <label className="block text-xs font-semibold text-slate-700 mb-1"> Country <span className="text-red-500">*</span> </label> <input type="text" value={country} onChange={(e) => setCountry(e.target.value)} placeholder="e.g. Nigeria" className="w-full px-3 py-2 rounded-xl border border-slate-300 text-sm focus:ring-2 focus:ring-amber-500" /> </div> </div> <div> <label className="block text-xs font-semibold text-slate-700 mb-1"> Church Website or Ministry Social Handle (Optional) </label> <div className="relative"> <input type="url" value={websiteOrSocial} onChange={(e) => setWebsiteOrSocial(e.target.value)} placeholder="https://churchwebsite.org or @churchhandle" className="w-full pl-9 pr-3 py-2 rounded-xl border border-slate-300 text-sm focus:ring-2 focus:ring-amber-500" /> <Globe className="w-4 h-4 text-slate-400 absolute left-3 top-2.5" /> </div> </div> </div> {/* Action Buttons */} <div className="flex flex-col sm:flex-row items-center justify-between gap-3 pt-4 border-t border-slate-200"> <button type="button" onClick={onClose} className="w-full sm:w-auto px-5 py-2.5 border border-slate-300 text-slate-700 hover:bg-slate-100 rounded-xl text-sm font-semibold transition cursor-pointer" > Cancel </button> <button type="submit" disabled={submitting} className="w-full sm:w-auto px-7 py-3 bg-gradient-to-r from-amber-600 via-amber-500 to-amber-600 hover:from-amber-700 hover:to-amber-700 text-white font-bold rounded-xl shadow-lg shadow-amber-600/25 flex items-center justify-center gap-2 transition cursor-pointer disabled:opacity-60 text-sm" > {submitting ? ( <> <div className="w-4 h-4 border-2 border-white border-t-transparent rounded-full animate-spin" /> <span>Registering Leader Profile...</span> </> ) : ( <> <CheckCircle className="w-4 h-4" /> <span>Submit & Join Leaders Directory</span> </> )} </button> </div> </form> </div> </div> );
};
