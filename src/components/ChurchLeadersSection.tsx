import React, { useState } from 'react';
import { Users, Search, Plus, MapPin, Church, Award, CheckCircle2, ArrowRight, Sparkles, Filter, ChevronRight, BookOpen, UserCheck
} from 'lucide-react';
import { ChurchLeader, MemberUser } from '../types';
import { LeaderRegistrationModal } from './LeaderRegistrationModal';
import { LeaderDetailModal } from './LeaderDetailModal'; interface ChurchLeadersSectionProps { leaders: ChurchLeader[]; onAddLeaderSuccess: (newLeader: ChurchLeader) => void; currentMember: MemberUser | null; onRequestSignIn: () => void;
} const CATEGORY_FILTERS = [ 'All Leaders', 'Senior Pastors / General Overseers', 'Bishops & Apostles', 'Reverends & Teachers', 'Youth & Campus Pastors', 'Worship & Levite Directors',
]; export const ChurchLeadersSection: React.FC<ChurchLeadersSectionProps> = ({ leaders, onAddLeaderSuccess, currentMember, onRequestSignIn,
}) => { const [searchQuery, setSearchQuery] = useState(''); const [selectedCategory, setSelectedCategory] = useState('All Leaders'); const [isRegisterModalOpen, setIsRegisterModalOpen] = useState(false); const [selectedLeader, setSelectedLeader] = useState<ChurchLeader | null>(null); const filteredLeaders = leaders.filter((leader) => { const query = searchQuery.toLowerCase().trim(); const matchesSearch = !query || leader.fullName.toLowerCase().includes(query) || leader.churchName.toLowerCase().includes(query) || leader.keyPosition.toLowerCase().includes(query) || leader.city.toLowerCase().includes(query) || (leader.positionDescription && leader.positionDescription.toLowerCase().includes(query)); let matchesCategory = true; if (selectedCategory === 'Senior Pastors / General Overseers') { matchesCategory = leader.keyPosition.toLowerCase().includes('senior') || leader.keyPosition.toLowerCase().includes('overseer'); } else if (selectedCategory === 'Bishops & Apostles') { matchesCategory = leader.title === 'Bishop' || leader.title === 'Apostle' || leader.keyPosition.toLowerCase().includes('bishop') || leader.keyPosition.toLowerCase().includes('apostle'); } else if (selectedCategory === 'Reverends & Teachers') { matchesCategory = leader.title === 'Reverend' || leader.title === 'Teacher' || leader.title === 'Dr.'; } else if (selectedCategory === 'Youth & Campus Pastors') { matchesCategory = leader.keyPosition.toLowerCase().includes('youth') || leader.keyPosition.toLowerCase().includes('campus'); } else if (selectedCategory === 'Worship & Levite Directors') { matchesCategory = leader.keyPosition.toLowerCase().includes('worship') || leader.keyPosition.toLowerCase().includes('music'); } return matchesSearch && matchesCategory; }); const handleOpenRegister = () => { if (!currentMember) { onRequestSignIn(); } else { setIsRegisterModalOpen(true); } }; return ( <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8"> {/* Hero Banner for Leaders Segment */} <div className="bg-gradient-to-r from-slate-900 via-amber-950 to-slate-900 text-white rounded-2xl p-6 sm:p-10 mb-8 border border-amber-900/40 shadow-lg relative overflow-hidden"> <div className="absolute -right-10 -bottom-10 opacity-10 pointer-events-none"> <Users className="w-80 h-80 text-amber-300" /> </div> <div className="relative z-10 max-w-3xl"> <div className="inline-flex items-center gap-2 px-3.5 py-1 rounded-full bg-amber-500/20 text-amber-300 border border-amber-500/30 text-xs font-bold uppercase tracking-wider mb-3"> <Sparkles className="w-3.5 h-3.5" /> <span>Presbytery & Kingdom Leaders Registry</span> </div> <h1 className="text-2xl sm:text-4xl font-bold font-serif tracking-tight text-white"> Church Leaders & Pastoral Directory </h1> <p className="text-slate-300 text-sm sm:text-base mt-2.5 leading-relaxed"> Discover accredited senior pastors, apostolic fathers, general overseers, and ministry executives. Leaders can register their profile, upload their official portrait, and describe their key pastoral calling and oversight. </p> {/* Call to action */} <div className="mt-6 flex flex-wrap items-center gap-3"> <button type="button" onClick={handleOpenRegister} className="px-6 py-3 bg-gradient-to-r from-amber-600 to-amber-500 hover:from-amber-700 hover:to-amber-600 text-white font-bold rounded-xl text-sm shadow-md shadow-amber-600/30 flex items-center gap-2 transition transform hover:-translate-y-0.5 cursor-pointer" > <Plus className="w-4 h-4" /> <span>Register as Church Leader</span> </button> {!currentMember && ( <button type="button" onClick={onRequestSignIn} className="px-5 py-3 bg-white/10 hover:bg-white/20 text-white font-medium rounded-xl text-xs sm:text-sm border border-white/20 transition cursor-pointer flex items-center gap-1.5" > <UserCheck className="w-4 h-4 text-amber-400" /> <span>Member Sign In Required to Register</span> </button> )} </div> </div> </div> {/* Search & Category Filter Bar */} <div className="bg-white border border-slate-200/80 rounded-2xl p-4 sm:p-6 shadow-xs mb-8 space-y-4"> <div className="flex flex-col md:flex-row gap-3 items-center justify-between"> {/* Search Input */} <div className="relative w-full md:w-96"> <input type="text" value={searchQuery} onChange={(e) => setSearchQuery(e.target.value)} placeholder="Search by leader name, church, city, or key role..." className="w-full pl-10 pr-4 py-2.5 rounded-xl border border-slate-300 text-sm focus:ring-2 focus:ring-amber-500 transition" /> <Search className="w-4 h-4 text-slate-400 absolute left-3.5 top-3" /> </div> <div className="flex items-center gap-2 w-full md:w-auto justify-between md:justify-end"> <span className="text-xs font-semibold text-slate-500"> Showing <strong className="text-slate-900">{filteredLeaders.length}</strong> of {leaders.length} Leaders </span> <button type="button" onClick={handleOpenRegister} className="hidden sm:inline-flex items-center gap-1.5 px-4 py-2 bg-amber-50 hover:bg-amber-100 text-amber-800 text-xs font-bold rounded-xl border border-amber-300 transition cursor-pointer" > <Plus className="w-3.5 h-3.5" /> <span>Join Directory</span> </button> </div> </div> {/* Category Pills */} <div className="flex items-center gap-1.5 overflow-x-auto pb-1 text-xs"> {CATEGORY_FILTERS.map((cat) => ( <button key={cat} type="button" onClick={() => setSelectedCategory(cat)} className={`px-3.5 py-1.5 rounded-xl font-semibold whitespace-nowrap transition cursor-pointer ${ selectedCategory === cat ? 'bg-slate-900 text-white shadow-xs' : 'bg-slate-100 text-slate-600 hover:bg-slate-200' }`} > {cat} </button> ))} </div> </div> {/* Leaders Grid */} {filteredLeaders.length > 0 ? ( <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6"> {filteredLeaders.map((leader) => ( <div key={leader.id} className="bg-white border border-slate-200/80 hover:border-amber-400 rounded-2xl p-5 shadow-xs hover:shadow-md transition duration-200 flex flex-col justify-between group" > <div> {/* Header Profile with Portrait Picture */} <div className="flex items-start gap-3.5 mb-4"> <div className="relative shrink-0"> <img src={leader.photoUrl || 'https://images.unsplash.com/photo-1507003211169-0a1dd7228f2d?auto=format&fit=crop&w=300&q=80'} alt={leader.fullName} referrerPolicy="no-referrer" className="w-16 h-16 sm:w-20 sm:h-20 rounded-2xl object-cover border-2 border-amber-500/40 shadow-sm bg-slate-100" /> {leader.isVerified && ( <div className="absolute -bottom-1 -right-1 bg-amber-500 text-white p-0.5 rounded-full border-2 border-white" title="Verified Presbytery Leader"> <CheckCircle2 className="w-3.5 h-3.5" /> </div> )} </div> <div className="flex-1 min-w-0"> <span className="inline-block px-2 py-0.5 rounded-md bg-amber-50 text-amber-800 border border-amber-200/60 text-[11px] font-bold truncate max-w-full mb-1"> {leader.keyPosition} </span> <h3 className="text-base sm:text-lg font-bold font-serif text-slate-900 group-hover:text-amber-700 transition truncate"> {leader.title} {leader.fullName} </h3> <p className="text-xs text-slate-600 flex items-center gap-1 truncate mt-0.5"> <Church className="w-3 h-3 text-amber-600 shrink-0" /> <span className="truncate">{leader.churchName}</span> </p> <p className="text-[11px] text-slate-400 flex items-center gap-1 mt-0.5"> <MapPin className="w-3 h-3 text-slate-400 shrink-0" /> <span>{leader.city}, {leader.country}</span> </p> </div> </div> {/* Written Description of Key Position (Excerpts) */} <div className="bg-slate-50 border border-slate-100 rounded-xl p-3.5 mb-4 text-xs text-slate-700 leading-relaxed line-clamp-3"> <p className="italic font-serif"> "{leader.positionDescription}" </p> </div> {/* Ministry Focus Tags */} {leader.ministryFocus && leader.ministryFocus.length > 0 && ( <div className="flex flex-wrap gap-1 mb-4"> {leader.ministryFocus.slice(0, 3).map((focus, idx) => ( <span key={idx} className="px-2 py-0.5 rounded bg-slate-100 text-slate-600 text-[10px] font-medium" > {focus} </span> ))} {leader.ministryFocus.length > 3 && ( <span className="px-1.5 py-0.5 rounded bg-slate-100 text-slate-500 text-[10px] font-bold"> +{leader.ministryFocus.length - 3} </span> )} </div> )} </div> {/* Card Footer Button */} <div className="pt-3 border-t border-slate-100 flex items-center justify-between"> <span className="text-[11px] font-semibold text-slate-500"> {leader.yearsInMinistry ? `${leader.yearsInMinistry} yrs in ministry` : 'Accredited'} </span> <button type="button" onClick={() => setSelectedLeader(leader)} className="inline-flex items-center gap-1 text-xs font-bold text-amber-700 hover:text-amber-800 group-hover:translate-x-0.5 transition cursor-pointer" > <span>View Full Calling</span> <ArrowRight className="w-3.5 h-3.5" /> </button> </div> </div> ))} </div> ) : ( <div className="text-center py-16 bg-white border border-slate-200/80 rounded-2xl p-8"> <Users className="w-12 h-12 text-slate-300 mx-auto mb-3" /> <h3 className="text-lg font-bold text-slate-800">No Church Leaders Found</h3> <p className="text-sm text-slate-500 max-w-md mx-auto mt-1 mb-5"> We couldn't find any registered church leaders matching your search filters. </p> <button type="button" onClick={handleOpenRegister} className="px-5 py-2.5 bg-amber-600 hover:bg-amber-700 text-white text-xs font-bold rounded-xl cursor-pointer" > Register the First Leader Here </button> </div> )} {/* Modals */} <LeaderRegistrationModal isOpen={isRegisterModalOpen} onClose={() => setIsRegisterModalOpen(false)} onSuccess={onAddLeaderSuccess} currentMember={currentMember} /> <LeaderDetailModal leader={selectedLeader} onClose={() => setSelectedLeader(null)} /> </div> );
};
