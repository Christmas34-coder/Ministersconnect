import React, { useState } from 'react';
import { Calendar, MapPin, Users, Search, Filter, ArrowRight, Sparkles, Flame, Clock, Info, CheckCircle2
} from 'lucide-react';
import { Programme, ProgrammeCategory } from '../types'; interface UpcomingProgrammesSectionProps { programmes: Programme[]; onSelectProgrammeForDetails: (programme: Programme) => void; onSelectProgrammeForRegister: (programmeId: string) => void;
} const CATEGORIES: ProgrammeCategory[] = [ 'All', 'Conferences', 'Leadership Summits', 'Ministers Retreats', 'Pastoral Workshops', 'Worship Conclaves', 'Youth Summits',
]; export const UpcomingProgrammesSection: React.FC<UpcomingProgrammesSectionProps> = ({ programmes, onSelectProgrammeForDetails, onSelectProgrammeForRegister,
}) => { const [selectedCategory, setSelectedCategory] = useState<ProgrammeCategory>('All'); const [searchQuery, setSearchQuery] = useState(''); const filteredProgrammes = programmes.filter((p) => { const matchesCategory = selectedCategory === 'All' || p.category === selectedCategory; const matchesSearch = p.title.toLowerCase().includes(searchQuery.toLowerCase()) || p.theme.toLowerCase().includes(searchQuery.toLowerCase()) || p.city.toLowerCase().includes(searchQuery.toLowerCase()) || p.venue.toLowerCase().includes(searchQuery.toLowerCase()); return matchesCategory && matchesSearch; }); return ( <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12"> {/* Section Header */} <div className="flex flex-col md:flex-row md:items-end justify-between mb-8 gap-4"> <div> <div className="inline-flex items-center gap-1.5 px-3 py-1 rounded-full bg-amber-100 text-amber-900 border border-amber-300/60 text-xs font-bold uppercase tracking-wider mb-2"> <Flame className="w-3.5 h-3.5 text-amber-600" /> Sacred Programmes & Gatherings </div> <h2 className="text-2xl sm:text-3xl font-extrabold font-serif text-slate-900 tracking-tight"> Upcoming Ministry Programmes & Summits </h2> <p className="text-sm text-slate-600 mt-1 max-w-2xl"> Explore sacred assemblies curated for apostolic renewal, pastoral refreshment, church administration, and levitical worship. </p> </div> {/* Search Bar */} <div className="relative w-full md:w-72"> <input type="text" value={searchQuery} onChange={(e) => setSearchQuery(e.target.value)} placeholder="Search by title, city, theme..." className="w-full pl-9 pr-4 py-2.5 rounded-xl border border-slate-300 bg-white text-xs sm:text-sm focus:ring-2 focus:ring-amber-500 focus:border-amber-500 transition shadow-2xs" /> <Search className="w-4 h-4 text-slate-400 absolute left-3 top-3.5" /> </div> </div> {/* Category Pills Filter */} <div className="flex items-center gap-2 overflow-x-auto pb-4 mb-8 no-scrollbar"> {CATEGORIES.map((category) => ( <button key={category} onClick={() => setSelectedCategory(category)} className={`px-4 py-2 rounded-xl text-xs sm:text-sm font-semibold whitespace-nowrap transition cursor-pointer ${ selectedCategory === category ? 'bg-amber-600 text-white shadow-sm shadow-amber-600/20' : 'bg-white text-slate-600 border border-slate-200 hover:bg-slate-50 hover:text-slate-900' }`} > {category} </button> ))} </div> {/* Programmes Grid */} {filteredProgrammes.length === 0 ? ( <div className="bg-white border border-slate-200 rounded-2xl p-12 text-center max-w-md mx-auto"> <Calendar className="w-12 h-12 text-slate-300 mx-auto mb-3" /> <h3 className="text-lg font-bold text-slate-800">No Programmes Found</h3> <p className="text-xs text-slate-500 mt-1 mb-4"> Try adjusting your search criteria or category filter. </p> <button onClick={() => { setSelectedCategory('All'); setSearchQuery(''); }} className="px-4 py-2 bg-amber-500 hover:bg-amber-600 text-slate-950 text-xs font-bold rounded-lg transition" > Reset Filters </button> </div> ) : ( <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 lg:gap-8"> {filteredProgrammes.map((programme) => { const capacityPercent = Math.min( 100, Math.round((programme.registeredCount / programme.capacity) * 100) ); return ( <div key={programme.id} className="bg-white border border-slate-200/90 rounded-2xl overflow-hidden shadow-xs hover:shadow-md transition-all flex flex-col group" > {/* Card Banner Image */} <div className="relative h-48 bg-slate-900 overflow-hidden"> <img src={programme.bannerUrl} alt={programme.title} className="w-full h-full object-cover group-hover:scale-105 transition-transform duration-500 opacity-80" referrerPolicy="no-referrer" /> <div className="absolute inset-0 bg-gradient-to-t from-slate-950/90 via-slate-950/20 to-transparent" /> {/* Top Badges */} <div className="absolute top-3 left-3 right-3 flex items-center justify-between"> <span className="px-2.5 py-0.5 rounded-full bg-slate-900/90 text-amber-300 font-bold text-[11px] border border-amber-400/30"> {programme.category} </span> {programme.isFeatured && ( <span className="px-2 py-0.5 rounded-full bg-amber-500 text-slate-950 font-extrabold text-[10px] uppercase tracking-wider flex items-center gap-1"> <Sparkles className="w-3 h-3" /> Featured </span> )} </div> {/* Dates on Image Bottom */} <div className="absolute bottom-3 left-3 right-3 text-white"> <div className="flex items-center gap-1.5 text-xs text-amber-300 font-medium"> <Calendar className="w-3.5 h-3.5" /> <span>{programme.startDate} to {programme.endDate}</span> </div> </div> </div> {/* Card Body */} <div className="p-5 sm:p-6 flex-1 flex flex-col justify-between space-y-4"> <div> <h3 className="text-lg font-bold font-serif text-slate-900 line-clamp-2 group-hover:text-amber-700 transition-colors"> {programme.title} </h3> <p className="text-xs font-serif italic text-amber-900 mt-1 line-clamp-1"> Theme: "{programme.theme}" </p> <p className="text-xs text-slate-600 mt-2.5 line-clamp-2 leading-relaxed"> {programme.description} </p> {/* Venue and City */} <div className="mt-4 space-y-1.5 text-xs text-slate-500"> <div className="flex items-center gap-2"> <MapPin className="w-3.5 h-3.5 text-amber-600 shrink-0" /> <span className="truncate">{programme.venue}, {programme.city}</span> </div> <div className="flex items-center gap-2"> <Clock className="w-3.5 h-3.5 text-amber-600 shrink-0" /> <span>{programme.time}</span> </div> </div> </div> {/* Capacity Bar & Inclusions */} <div className="pt-3 border-t border-slate-100 space-y-2"> <div className="flex items-center justify-between text-[11px] text-slate-500 font-medium"> <span>{programme.registeredCount} Ministers Registered</span> <span className="font-semibold text-slate-700">{capacityPercent}% Capacity</span> </div> <div className="w-full bg-slate-100 h-1.5 rounded-full overflow-hidden"> <div className="bg-amber-600 h-full rounded-full transition-all" style={{ width: `${capacityPercent}%` }} /> </div> </div> {/* Action Buttons */} <div className="pt-2 flex items-center gap-2"> <button onClick={() => onSelectProgrammeForDetails(programme)} className="flex-1 py-2.5 px-3 bg-slate-100 hover:bg-slate-200 text-slate-800 rounded-xl text-xs font-semibold transition cursor-pointer flex items-center justify-center gap-1" > <Info className="w-3.5 h-3.5" /> <span>Details & Schedule</span> </button> <button onClick={() => onSelectProgrammeForRegister(programme.id)} className="flex-1 py-2.5 px-3 bg-amber-600 hover:bg-amber-700 text-white rounded-xl text-xs font-bold transition shadow-xs cursor-pointer flex items-center justify-center gap-1" > <span>Register</span> <ArrowRight className="w-3.5 h-3.5" /> </button> </div> </div> </div> ); })} </div> )} </div> );
};
